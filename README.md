This project is a simple flashcards app using react and redux


---------------------------------------------------------------

git clone the project and run 'yarn start'

access the app from http://localhost:3000
